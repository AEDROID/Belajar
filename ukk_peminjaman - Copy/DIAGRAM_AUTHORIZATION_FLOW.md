# 🔐 Diagram Authorization Flow - CRUD User

## 📊 Triple Layer Protection Flow

```
┌─────────────────────────────────────────────────────────────────────────┐
│                         USER MENGAKSES /users                           │
└─────────────────────────────────────────────────────────────────────────┘
                                    │
                                    ▼
┌─────────────────────────────────────────────────────────────────────────┐
│                    LAYER 1: MIDDLEWARE CHECK                            │
│                    Route: role:admin                                    │
├─────────────────────────────────────────────────────────────────────────┤
│                                                                         │
│  if (user->role !== 'admin') {                                         │
│      return 403 Forbidden ❌                                            │
│  }                                                                      │
│                                                                         │
└─────────────────────────────────────────────────────────────────────────┘
                                    │
                                    ▼ (Admin ✅)
┌─────────────────────────────────────────────────────────────────────────┐
│                    LAYER 2: CONTROLLER CHECK                            │
│                    $this->authorize('Admin')                            │
├─────────────────────────────────────────────────────────────────────────┤
│                                                                         │
│  Gate::check('Admin') {                                                │
│      if (user->role !== 'admin') {                                     │
│          throw AuthorizationException ❌                                │
│      }                                                                  │
│  }                                                                      │
│                                                                         │
└─────────────────────────────────────────────────────────────────────────┘
                                    │
                                    ▼ (Admin ✅)
┌─────────────────────────────────────────────────────────────────────────┐
│                    LAYER 3: VIEW CHECK                                  │
│                    @can('Admin')                                        │
├─────────────────────────────────────────────────────────────────────────┤
│                                                                         │
│  @can('Admin')                                                          │
│      <!-- Tampilkan konten admin -->                                   │
│      <table>...</table>                                                │
│      <button>Tambah User</button>                                      │
│  @else                                                                  │
│      <!-- Tampilkan pesan error -->                                    │
│      <div>Akses Ditolak!</div>                                         │
│  @endcan                                                                │
│                                                                         │
└─────────────────────────────────────────────────────────────────────────┘
                                    │
                                    ▼ (Admin ✅)
┌─────────────────────────────────────────────────────────────────────────┐
│                    TAMPILKAN HALAMAN USERS                              │
│                    dengan semua fitur CRUD                              │
└─────────────────────────────────────────────────────────────────────────┘
```

---

## 🔄 Flow Diagram untuk Setiap Role

### 1️⃣ Admin Flow ✅

```
Admin Login
    │
    ▼
Akses /users
    │
    ├─→ Middleware: role:admin ✅ PASS
    │
    ├─→ Controller: authorize('Admin') ✅ PASS
    │
    ├─→ View: @can('Admin') ✅ PASS
    │
    ▼
Tampilkan Halaman Users
    │
    ├─→ Tabel daftar user
    ├─→ Tombol Tambah User
    ├─→ Tombol Edit
    └─→ Tombol Hapus
```

### 2️⃣ Petugas Flow ❌

```
Petugas Login
    │
    ▼
Akses /users
    │
    ├─→ Middleware: role:admin ❌ FAIL
    │
    ▼
403 Forbidden
    │
    └─→ "You are not authorized to access this page"
```

### 3️⃣ User Flow ❌

```
User Login
    │
    ▼
Akses /users
    │
    ├─→ Middleware: role:admin ❌ FAIL
    │
    ▼
403 Forbidden
    │
    └─→ "You are not authorized to access this page"
```

---

## 🎯 Gate Definition Flow

```
┌─────────────────────────────────────────────────────────────────────────┐
│                    AppServiceProvider.php                               │
│                    boot() method                                        │
├─────────────────────────────────────────────────────────────────────────┤
│                                                                         │
│  Gate::define('Admin', function($user) {                               │
│      return $user->role == 'admin';                                    │
│  });                                                                    │
│                                                                         │
│  Gate::define('Petugas', function($user) {                             │
│      return $user->role == 'petugas';                                  │
│  });                                                                    │
│                                                                         │
│  Gate::define('User', function($user) {                                │
│      return $user->role == 'user';                                     │
│  });                                                                    │
│                                                                         │
└─────────────────────────────────────────────────────────────────────────┘
                                    │
                                    ▼
┌─────────────────────────────────────────────────────────────────────────┐
│                    Gate tersedia di seluruh aplikasi                    │
├─────────────────────────────────────────────────────────────────────────┤
│                                                                         │
│  Controller: $this->authorize('Admin')                                 │
│  View: @can('Admin')                                                    │
│  Code: Gate::allows('Admin')                                           │
│                                                                         │
└─────────────────────────────────────────────────────────────────────────┘
```

---

## 🔀 CRUD Operations Flow

### CREATE (Tambah User)

```
Admin → Klik "Tambah User"
    │
    ▼
GET /users/create
    │
    ├─→ Middleware ✅
    ├─→ Controller: authorize('Admin') ✅
    ├─→ View: @can('Admin') ✅
    │
    ▼
Tampilkan Form
    │
    ▼
Admin isi form & submit
    │
    ▼
POST /users
    │
    ├─→ Middleware ✅
    ├─→ Controller: authorize('Admin') ✅
    ├─→ Validasi input
    ├─→ Hash password
    ├─→ Simpan ke database
    │
    ▼
Redirect ke /users dengan success message
```

### READ (Lihat Daftar User)

```
Admin → Klik menu "Users"
    │
    ▼
GET /users
    │
    ├─→ Middleware ✅
    ├─→ Controller: authorize('Admin') ✅
    ├─→ Load semua user dari database
    ├─→ View: @can('Admin') ✅
    │
    ▼
Tampilkan tabel daftar user
```

### UPDATE (Edit User)

```
Admin → Klik "Edit" pada user
    │
    ▼
GET /users/{id}/edit
    │
    ├─→ Middleware ✅
    ├─→ Controller: authorize('Admin') ✅
    ├─→ Load user dari database
    ├─→ View: @can('Admin') ✅
    │
    ▼
Tampilkan Form dengan data user
    │
    ▼
Admin ubah data & submit
    │
    ▼
PUT /users/{id}
    │
    ├─→ Middleware ✅
    ├─→ Controller: authorize('Admin') ✅
    ├─→ Validasi input
    ├─→ Hash password (jika diisi)
    ├─→ Update database
    │
    ▼
Redirect ke /users dengan success message
```

### DELETE (Hapus User)

```
Admin → Klik "Hapus" pada user
    │
    ▼
Konfirmasi: "Apakah yakin?"
    │
    ├─→ Batal → Kembali ke /users
    │
    ▼ (Ya)
DELETE /users/{id}
    │
    ├─→ Middleware ✅
    ├─→ Controller: authorize('Admin') ✅
    ├─→ Cek: user_id !== auth()->id()
    │   ├─→ Jika sama → Error ❌
    │   └─→ Jika beda → Lanjut ✅
    ├─→ Hapus dari database
    │
    ▼
Redirect ke /users dengan success message
```

---

## 🛡️ Security Layers Diagram

```
┌─────────────────────────────────────────────────────────────────────────┐
│                         SECURITY LAYERS                                 │
├─────────────────────────────────────────────────────────────────────────┤
│                                                                         │
│  ┌───────────────────────────────────────────────────────────────────┐ │
│  │ Layer 1: MIDDLEWARE (Route Level)                                │ │
│  │ - Cek role di route                                              │ │
│  │ - Jika bukan admin → 403 Forbidden                               │ │
│  └───────────────────────────────────────────────────────────────────┘ │
│                              │                                          │
│                              ▼ (Admin ✅)                                │
│  ┌───────────────────────────────────────────────────────────────────┐ │
│  │ Layer 2: CONTROLLER (Logic Level)                                │ │
│  │ - authorize('Admin')                                             │ │
│  │ - Jika bukan admin → AuthorizationException                      │ │
│  └───────────────────────────────────────────────────────────────────┘ │
│                              │                                          │
│                              ▼ (Admin ✅)                                │
│  ┌───────────────────────────────────────────────────────────────────┐ │
│  │ Layer 3: VIEW (UI Level)                                         │ │
│  │ - @can('Admin')                                                  │ │
│  │ - Jika bukan admin → Tampilkan pesan error                       │ │
│  └───────────────────────────────────────────────────────────────────┘ │
│                              │                                          │
│                              ▼ (Admin ✅)                                │
│  ┌───────────────────────────────────────────────────────────────────┐ │
│  │ TAMPILKAN KONTEN                                                 │ │
│  └───────────────────────────────────────────────────────────────────┘ │
│                                                                         │
└─────────────────────────────────────────────────────────────────────────┘
```

---

## 📋 Navigation Menu Flow

```
User Login
    │
    ▼
Load Navigation Menu
    │
    ├─→ @can('Admin')
    │   │
    │   ├─→ Dashboard
    │   ├─→ Users ← Hanya untuk admin
    │   ├─→ Kategori
    │   ├─→ Alat
    │   ├─→ Peminjaman
    │   └─→ Log Aktivitas
    │
    ├─→ @can('Petugas')
    │   │
    │   ├─→ Dashboard
    │   └─→ Peminjaman
    │
    └─→ @can('User')
        │
        ├─→ Dashboard
        └─→ Peminjaman
```

---

## 🔄 Request-Response Cycle

```
┌─────────────────────────────────────────────────────────────────────────┐
│                         REQUEST-RESPONSE CYCLE                          │
└─────────────────────────────────────────────────────────────────────────┘

Browser                 Laravel                 Database
   │                       │                        │
   │  GET /users          │                        │
   ├──────────────────────►│                        │
   │                       │                        │
   │                       │ Middleware Check       │
   │                       │ (role:admin)           │
   │                       │                        │
   │                       │ Controller Check       │
   │                       │ (authorize('Admin'))   │
   │                       │                        │
   │                       │  SELECT * FROM users   │
   │                       ├───────────────────────►│
   │                       │                        │
   │                       │  Return users data     │
   │                       │◄───────────────────────┤
   │                       │                        │
   │                       │ View Check             │
   │                       │ (@can('Admin'))        │
   │                       │                        │
   │  HTML Response        │                        │
   │◄──────────────────────┤                        │
   │                       │                        │
   ▼                       ▼                        ▼
Display                 Done                    Done
```

---

## 🎯 Summary

### Triple Layer Protection
1. **Middleware** → Cek di route level
2. **Controller** → Cek di logic level
3. **View** → Cek di UI level

### Keuntungan
- ✅ **Defense in Depth** - Keamanan berlapis
- ✅ **User Friendly** - Pesan error yang jelas
- ✅ **Maintainable** - Logic terpusat di Gate
- ✅ **Reusable** - Gate bisa digunakan di mana saja

---

**Dengan diagram ini, flow authorization CRUD User sudah jelas! 🔐**
